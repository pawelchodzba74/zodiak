import { RoutDirective } from './rout.directive';

describe('RoutDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutDirective();
    expect(directive).toBeTruthy();
  });
});
