import { ShowHideHeadersDirective } from './show-hide-headers.directive';

describe('ShowHideHeadersDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideHeadersDirective();
    expect(directive).toBeTruthy();
  });
});
