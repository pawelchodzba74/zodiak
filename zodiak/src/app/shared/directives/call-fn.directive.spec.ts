import { CallFnDirective } from './call-fn.directive';

describe('CallFnDirective', () => {
  it('should create an instance', () => {
    const directive = new CallFnDirective();
    expect(directive).toBeTruthy();
  });
});
